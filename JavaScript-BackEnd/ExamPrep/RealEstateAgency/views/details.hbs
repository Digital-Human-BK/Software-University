{{#with listing}}
<section id="deatils-page">
    <div class="wrapper">
        <div class="product-img">
            <img src="{{img}}">
        </div>
        <div class="product-info">
            <div class="product-text">
                <div class="product-text">
                    <h1>Name: {{name}}</h1>
                    <h2>Type: {{type}}</h2>
                    <h4>Year: {{year}}</h4>
                    <h4>City: {{city}}</h4>
                    <p>Description: {{description}}</p>

                    {{#if tenantsList}}
                    <p>People rented this housing: {{tenantsList}}</p>
                    {{else}}
                    <p>People rented this housing: There are no tenants yet.</p>
                    {{/if}}
                </div>
            </div>

            <!-- if there is no registered user, do not display buttons-->
            {{#if hasUser}}
            <div class="product-btn">
                {{#if isOwner}}
                <!-- Only for registered user and creator of the housing offer-->
                <a href="/edit/{{_id}}" class="edit">Edit</a>
                <a href="/delete/{{_id}}" class="remove">Delete</a>
                {{else}}
                <!-- logged in user with available pieces-->
                    {{#if isRented}}
                    <p class="alRentHome">You have already rent this home</p>
                    {{else}}
                        {{#if remainingPlaces}}
                        <a href="/rent/{{_id}}" class="rentHome">Rent a home, available {{remainingPlaces}} housing</a>
                        {{else}}    
                        <p class="no-housing">No Housing Available!</p>
                        {{/if}}    
                    {{/if}}
                {{/if}}
            </div>
            {{/if}}
        </div>
    </div>
</section>
{{/with}}