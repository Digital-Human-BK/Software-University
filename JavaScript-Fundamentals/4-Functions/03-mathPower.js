function mathPow(n, exponent) {

  /* let result = 1;
  for (let i = 0; i < exponent; i++) {
    result *= n
   } */

  // let result = n ** exponent;
    
  let result = Math.pow(n, exponent)
  console.log(result);
}
mathPow(2, 8)
mathPow(3, 4)